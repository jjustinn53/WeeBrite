<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/login.css">
</head>
<body>
    <%- include('partials/header.ejs') %>

    <div class="profile-container">
        <h1>Update Profile</h1>
        <form action="/profile/save" method="POST">
            <div class="form-group">
                <label for="bio">Bio:</label>
                <textarea id="bio" name="bio" rows="4" cols="50" placeholder="<%= userInfo[0]?.bio || 'Write something about yourself...' %>"><%= userInfo[0]?.bio || '' %></textarea>
            </div>

            <div class="form-group">
                <label for="birthday">Birthday:</label>
                <input type="date" id="birthday" name="birthday" value="<%= userInfo[0]?.birth ? new Date(userInfo[0].birth).toISOString().split('T')[0] : '' %>" placeholder="Enter your birthday">
            </div>

            <button type="submit">Save</button>
        </form>

        <a id="logout" href="/logout">Logout</a>
    </div>

    <style>
        .profile-container {
            width: 100%;
            max-width: 450px;
            padding: 20px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .profile-container h1 {
            text-align: center;
            color: rgb(243, 108, 33);
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #333;
            font-size: 14px;
        }

        textarea, input[type="date"] {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        textarea:focus, input[type="date"]:focus {
            outline: none;
            border-color: rgb(243, 108, 33);
            box-shadow: 0 0 0 3px rgba(243, 108, 33, 0.1);
        }

        button {
            background: linear-gradient(135deg, rgb(243, 108, 33) 0%, rgb(255, 153, 85) 100%);
            color: white;
            border: none;
            padding: 14px;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.3s ease;
            margin-top: 10px;

            margin-bottom: 30px;
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(243, 108, 33, 0.3);
        }

        button:active {
            transform: translateY(0);
        }

        #logout {
            margin-top: 20px;
            padding: 14px;
            background: white;
            color: rgb(243, 108, 33);
            border: 2px solid rgb(243, 108, 33);
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        #logout:hover {
            background: rgb(243, 108, 33);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(243, 108, 33, 0.3);
        }

        #logout:active {
            transform: translateY(0);
        }
    </style>
</body>
</html>