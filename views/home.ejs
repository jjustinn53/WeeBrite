<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WeeBrite - Home</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Boldonse&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="css/login.css">
    <style>
        .welcome-section {
            max-width: 700px;
            margin: 20px auto;
            padding: 25px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .welcome-section h1 {
            color: rgb(243, 108, 33);
            font-size: 28px;
            margin-bottom: 15px;
            text-align: center;
        }

        .welcome-section p {
            color: #333;
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 12px;
        }

        .game-flow {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .flow-card {
            background: linear-gradient(135deg, rgb(243, 108, 33) 0%, rgb(255, 153, 85) 100%);
            padding: 15px;
            border-radius: 12px;
            color: white;
            text-align: center;
        }

        .flow-card h3 {
            font-size: 16px;
            margin-bottom: 8px;
        }

        .flow-card p {
            color: white;
            font-size: 12px;
            margin: 0;
        }

        .cta-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 25px;
            flex-wrap: wrap;
        }

        .cta-btn {
            background: rgb(243, 108, 33);
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 10px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
        }

        .cta-btn:hover {
            background: rgb(255, 153, 85);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(243, 108, 33, 0.3);
        }

        .cta-btn.secondary {
            background: white;
            color: rgb(243, 108, 33);
            border: 2px solid rgb(243, 108, 33);
        }

        .cta-btn.secondary:hover {
            background: rgb(243, 108, 33);
            color: white;
        }

        .search-section {
            max-width: 700px;
            width: 100%;
            margin: 20px auto;
            padding: 25px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        .search-section h2 {
            display: none;
        }

        .search-container {
            display: flex;
            gap: 10px;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .search-input {
            padding: 10px 15px;
            border: 2px solid rgb(243, 108, 33);
            border-radius: 8px;
            font-size: 14px;
            flex: 1;
            min-width: 200px;
            transition: border-color 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            border-color: rgb(255, 153, 85);
            box-shadow: 0 0 0 3px rgba(243, 108, 33, 0.1);
        }

        .search-btn {
            background: rgb(243, 108, 33);
            color: white;
            padding: 10px 25px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            white-space: nowrap;
        }

        .search-btn:hover {
            background: rgb(255, 153, 85);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(243, 108, 33, 0.3);
        }

        .featured-section {
            max-width: 900px;
            width: 100%;
            margin: 40px auto 20px;
            padding: 0 20px;
            box-sizing: border-box;
        }

        .featured-section h2 {
            color: rgb(243, 108, 33);
            font-size: 26px;
            text-align: center;
            margin-bottom: 30px;
        }

        .featured-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .featured-char {
            background: white;
            border: 1px solid #ddd;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            padding: 15px;
            min-height: 350px;
        }

        .featured-char:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(243, 108, 33, 0.2);
        }

        .featured-img {
            width: auto;
            height: 220px;
            object-fit: contain;
            margin-bottom: 15px;
            box-sizing: border-box;
        }

        .featured-img-placeholder {
            width: 100%;
            height: 220px;
            background: #f0f0f0;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 8px;
            color: #999;
            font-size: 14px;
            margin-bottom: 15px;
        }

        .featured-char h3 {
            color: rgb(243, 108, 33);
            font-size: 18px;
            margin-bottom: 8px;
        }

        .char-anime {
            color: #666;
            font-size: 12px;
            margin: 0;
        }
    </style>
</head>

<body style="padding-top: 120px;">
    <%- include('partials/header.ejs') %>
    
    <main class="welcome-section">
        <h1>Welcome to WeeBrite</h1>
        
        <p style="text-align: center;" >Welcome to <strong>WeeBrite</strong>, an interactive anime character collection game! Test your anime knowledge and build your ultimate squad.</p>
        
        <h2 style="color: rgb(243, 108, 33); font-size: 24px; margin-top: 30px; text-align: center;">How It Works</h2>
        
        <div class="game-flow">
            <div class="flow-card">
                <h3>Play</h3>
                <p>Answer anime quote questions to identify characters and unlock them</p>
            </div>
            <div class="flow-card">
                <h3>Unlock</h3>
                <p>Successfully guess characters to add them to your collection</p>
            </div>
            <div class="flow-card">
                <h3>Build Squad</h3>
                <p>Arrange your favorite unlocked characters into a custom squad</p>
            </div>
        </div>

        <div class="cta-buttons">
            <% if (authenticated) { %>
                <a href="/quiz" class="cta-btn">Start Playing</a>
                <a href="/squad" class="cta-btn secondary">View My Squad</a>
            <% } else { %>
                <a href="/signup" class="cta-btn">Create Account</a>
                <a href="/login" class="cta-btn secondary">Login</a>
            <% } %>
        </div>
    </main>

    <section class="search-section">
        <h2>Search Characters</h2>
        <form class="search-container" method="GET" action="/search">
            <input type="text" name="q" class="search-input" placeholder="Search for anime/characters" required>
            <button type="submit" class="search-btn">Search</button>
        </form>
    </section>

    <section class="featured-section">
        <h2>Featured Characters</h2>
        <div class="featured-container">
            <% if (featuredCharacters && featuredCharacters.length >= 3 ) { %>
                <% for (let i = 0; i < 3; i++) { %>
                    <% let char = featuredCharacters[i]; %>
                    <div class="featured-char">
                        <% if (char.image_url) { %>
                            <img src="<%= char.image_url %>" alt="<%= char.name %>" class="featured-img">
                        <% } else { %>
                            <div class="featured-img-placeholder">No Image</div>
                        <% } %>
                        <h3><%= char.name %></h3>
                        <p class="char-anime"><%= char.anime || 'Unknown Anime' %></p>
                    </div>
                <% } %>
            <% } %>
        </div>
    </section>
    
    <%- include('partials/footer.ejs') %>

</body>
</html>